{
  "name": "auth-idp",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @idp/api dev",
    "build": "pnpm --filter @idp/api build",
    "test": "pnpm -r test",
    "dev:admin": "pnpm --filter @idp/admin dev",
    "build:admin": "pnpm --filter @idp/admin build"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}